# NeutralTrade-Ai-Bot

This repository contains a trading bot and dashboard.

## Setup

1. Copy `trading_bot/.env.example` to `trading_bot/.env`
2. Fill in your Binance API keys and secret
3. Install dependencies

> Do not commit `trading_bot/.env` to Git. It is ignored by `.gitignore`.
